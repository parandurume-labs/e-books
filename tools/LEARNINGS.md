# 작업 학습 기록

> 다음 배치를 시작할 때 `STYLE.md` 와 함께 읽는다.
> gstack 의 `gstack-learnings-log` 가 이 환경에서 동작하지 않아(exit 1, 파일 미생성)
> 저장소에 직접 남긴다. 어차피 이 프로젝트 고유 지식이라 콘텐츠와 같이 있는 편이 낫다.

## 2026-07-30 · 공정을 4단계로 바꾼 회고에서

### L1. 판본 대조는 공짜 의미 검수기다 · 신뢰도 9

이중 언어판에서 **번역자는 원문의 깨진 문장을 뜻을 이해해 조용히 바로잡는다.**
ch03 의 `"아니요... 아직 아무것도 안 물어볼 수 있어요."` 를 영어판은
`"No... there is nothing to ask it yet."` 로 옮겼다. 그 영어 문장이 원문 의도를 복원하는
증거가 됐고, 한국어를 어떻게 고쳐야 하는지 알려줬다.

**적용:** 한국어판과 영어판의 뜻이 갈리는 지점을 결함 신호로 취급한다.
검수자에게 두 판본을 같이 주고 문단 단위로 대조하게 한다. 이건 추가 비용이 거의 없다.

### L2. 세는 검증은 뜻을 못 잡는다 · 신뢰도 9

지표 검증(em dash 개수, 태그 짝, 링크 존재, 요소 개수 일치, 겹침 좌표)은 **형식만** 본다.
부정이 뒤집힌 문장이 문체 교정(opus) → 구조 대조(haiku) → 영문 교정(haiku) → 렌더 QA
**네 관문을 전부 통과**했다. 넷 다 셈이었기 때문이다.

**적용:** 읽는 단계를 별도로 둔다. 그리고 **그 검수자는 교정한 사람과 달라야 한다.**
자기가 고친 문장은 뜻이 통해 보인다.

### L3. 교정 전 원본을 보관해야 diff 로 결백을 증명할 수 있다 · 신뢰도 8

일괄 교정 전 프래그먼트를 `frag-ko-archived/` 에 남겨둔 덕에, 검수자들이 기계 diff 로
**em dash 69건 제거가 뜻을 바꾸지 않았음**을 증명했다. 보관이 없으면 검수자가 모든 문장을
처음부터 판단해야 하고 비용이 몇 배가 된다.

**적용:** 일괄 교정 전에 원본을 별도 폴더로 복사한다. 빌드 스크립트가 그 폴더를
읽지 못하게 이름을 바꿔 봉인한다(그러지 않으면 교정 결과를 덮어쓴다. 실제로 위험했다).

### L4. 태그가 섞인 문장에 부분 치환을 하면 안 된다 · 신뢰도 9

`<code>`, `<strong>` 이 섞인 문장에 부분 문자열 치환을 했더니 결과가 태그 사이에 박혀
문장이 깨졌다. ch15 영어판 통제 어휘 문단에서 실제로 발생했고, 눈으로 확인해서 겨우 잡았다.
자동 검증은 태그 균형만 보므로 **통과했다.**

**적용:** 인라인 태그가 있는 문장은 문장 전체를 교체한다. 부분 치환 금지.

### L5. 다중 책 도구는 상대경로를 키로 써야 한다 · 신뢰도 9

이 저장소의 네 책은 모두 `chapters/ch01.html` 같은 같은 파일명을 쓴다.
집계를 파일명으로 키잉했더니 마지막에 처리된 책이 다른 책의 수치를 다 흡수해서
`claude-100x` 가 0 으로 나왔다. **조용히 틀린 집계**였다.

**적용:** `tools/check-consistency.py` 의 `rel()` 처럼 저장소 상대경로를 쓴다.

### L6. 개수 주장 대조는 정밀도가 0 이다 · 신뢰도 8

「세 가지」 같은 개수 주장과 실제 항목 수를 정규식으로 대조하는 검사를 만들었는데
**7건 전부 오탐**이었다. 「3개월」, 「1단계」, 「12개월 로드맵」, 「5단계 · 대시보드 한 장」처럼
서수와 기간이 같은 패턴에 걸린다.

**적용:** 이 유형에 스크립트를 더 투자하지 않는다. 사람이 읽어서 잡는 편이 싸다.
실제로 읽는 검수가 「세 번 고쳤습니다 / 표 4행」을 정확히 잡았다.

### L7. 불변식을 잘못된 범위에 적용하면 도구가 거짓말한다 · 신뢰도 9

`tools/invariants.json` 의 `structure`(장 19개, 부 7개)를 전역으로 뒀더니
다른 세 책에서 **오탐 35건**이 났다. 이 책만의 값을 네 책에 적용한 것이다.

역설적이다. 이 도구는 「단일 진실 공급원을 지켜라」를 검사하려고 만든 것인데,
만들면서 같은 실수를 했다. **불변식에는 반드시 유효 범위가 붙어야 한다.**

### L8. 비율 예산은 짧은 문서에서 노이즈다 · 신뢰도 8

`수 있습니다` 를 1만자당으로 환산했더니 `coming-soon.html`(짧은 페이지)에서
1건이 17.4회로 잡혔다. 분량 하한(3000자)을 두고 나서야 쓸 만해졌다.

## 2026-07-31 · 스킬 카탈로그와 용어 사전을 만들면서

### L9. 「골라 싣기」는 독자에게 「없음」과 구별되지 않는다 · 신뢰도 9

ch07 은 gstack 스킬 53개 중 26개만 골라 실었다. 나머지는 「특정 상황에서만 쓰는 것」이라고
한 줄로 처리했다. 그런데 **목록에 없는 스킬은 독자에게 존재하지 않는 것과 같다.**
`/spec`, `/diagram`, `/health`, `/learn` 처럼 초보자에게 바로 쓸모 있는 것들이 빠져 있었다.

**적용:** 카탈로그 성격의 장은 **전부 싣고 묶음으로 나눈다.** 분량이 부담이면 항목을 줄이지 말고
설명을 줄인다. 「먼저 배울 5개」 같은 진입점을 따로 두면 전체를 실어도 부담스럽지 않다.

### L10. 배지 색을 책 팔레트 변수로 만들면 안 된다 · 신뢰도 9

난이도 배지를 `--accent` / `--warn` 으로 칠했더니 gstack 입문서에서 **중급과 고급이
같은 색으로 보였다.** 이 책은 두 변수가 모두 주황 계열이기 때문이다
(라이트 `#B8542B` 와 `#B24A2E`, 다크 `#E89A70` 와 `#E8977A`).

책 팔레트는 **강조 하나와 경고 하나**를 위한 것이지, **서로 구별돼야 하는 3단 척도**를 위한 것이 아니다.
초록 → 노랑 → 빨강처럼 뜻이 정해진 색은 `.lv-*` 에서 직접 정하고 다크 모드만 따로 덮는다.

### L11. 검사 조건이 좁으면 표와 프론트매터를 통째로 놓친다 · 신뢰도 9

비정본 필드명 검사가 「`deadline` 뒤에 `필드`나 `field` 가 붙은 경우」만 봤다.
그래서 ch03 의 온톨로지 정의표 `| 기회 | OPP- | ... | stage, deadline |` 이 그냥 지나갔다.
**이 책은 ch15 에서 `deadline` 을 「틀린 필드 이름」의 대표 예로 쓰고 있었다.**
책이 자기 규칙을 어기고 있었고, 검사기는 통과시켰다.

**적용:** 필드명 검사는 **예제 코드 블록 안까지** 본다. 다만 판정은 판본에 따라 다르다.
한국어판은 코드 블록 안의 홀로 선 영단어를 필드명으로 봐도 되지만,
영어판에 같은 규칙을 쓰면 전부 오탐이다(`Submission deadline`, `an owner and a deadline`).
실제로 3건 전부 오탐이었다. 영어판은 `deadline:` 처럼 뜻이 하나뿐인 자리만 잡는다.

### L12. 고친 뒤에 되돌려서 다시 잡히는지 봐야 한다 · 신뢰도 8

L11 의 검사를 고치고 「ERROR 0」 을 확인했는데, 그건 **검사가 동작해서가 아니라
아무것도 안 봐서** 일 수도 있다. 버그를 일부러 되돌려 넣고 1건이 잡히는지,
되돌린 것을 복구하고 0건이 되는지 둘 다 확인해야 판정이 선다.

한 가지 사고가 있었다. 되돌리기 → 검사 → 복구를 한 스크립트에 넣었는데
중간에 인코딩 오류로 죽어서 **파일이 망가진 상태로 남았다.**
복구는 예외가 나도 반드시 실행되게 짜거나, 검사 결과를 읽기 전에 먼저 복구한다.

## 다음 배치에 그대로 쓸 것

| 자산 | 용도 |
|---|---|
| `STYLE.md` | 문체 규약 + 4단계 공정 + 경로/필드 처리 규칙 |
| `tools/invariants.json` | 책별 불변식. 새 책을 추가하면 `structure` 에 등록한다 |
| `tools/check-consistency.py` | 2·3단계 전후로 돌린다. ERROR 0 이 통과 조건 |
| `tools/build-glossary-llm-wiki.py` | 용어 사전 생성. 용어 목록만 갈아끼우면 다른 책에도 쓴다 |
| `tools/link-terms.py` | 본문 첫 등장 용어에 사전 링크. 태그 밖 텍스트만 건드린다 |
| `scratchpad/build.py` | 프래그먼트 → 챕터 조립, 언어별 목차, hreflang 상호 연결 |
| `frag-ko-archived/` | 교정 전 원본. diff 근거 |
