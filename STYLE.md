# 문체 규약 — 파란두루미 e-book 컬렉션

> 2026-07-30 신설. 한국어판의 번역투를 걷어내고, 영어판을 같은 목소리로 쓰기 위한 규약.
> 이 파일은 사람과 에이전트가 함께 본다. 새 장을 쓰거나 고칠 때 먼저 읽는다.

## 왜 이 파일이 생겼나

실측 결과 한국어판에 번역투 지표가 이만큼 있었다 (8개 장 · 50,408자 기준).

| 지표 | 횟수 | 1만자당 |
|---|---|---|
| em dash `—` | 127 | 25.2 |
| `~수 있습니다` | 24 | 4.8 |
| `~적(인)` | 24 | 4.8 |
| `~것입니다` | 22 | 4.4 |
| `여러분` | 17 | 3.4 |
| 문두 `그래서/그리고/그런데` | 27 | 5.4 |
| 피동 `~되어 있` | 12 | 2.4 |
| `~에 대해` | 11 | 2.2 |

영어를 한국어로 옮긴 글에서 나오는 패턴이다. 뜻은 통하지만 **한국어로 처음 쓴 글의 리듬이 아니다.**

## 1차 교정 결과 (2026-07-30, 8개 장)

| 지표 | 전 | 후 |
|---|---|---|
| em dash `—` | 127 | **0** |
| `여러분` | 17 | **0** |
| `~것입니다` | 22 | **3** |
| `~에 대해` | 11 | **3** |
| `~수 있습니다` | 24 | **15** |

`수 있습니다` 15건과 `것입니다` 3건은 **일부러 남긴 것**이다. 진짜 가능성·반사실 가정·가능/불가능 대조는
바꾸면 뜻이 달라진다. 이 규약의 목표는 지표를 0으로 만드는 게 아니라 **번역투 리듬을 없애는 것**이다.

---

# 제1부 · 한국어 규약

## 1. em dash 를 없앤다 (가장 중요)

`—` 는 영어 문장부호다. 한국어 산문에서 이걸 쓰면 즉시 번역문처럼 읽힌다.
**세 가지 방법으로 바꾼다. 셋 다 안 되면 문장을 둘로 쪼갠다.**

| 쓰임 | 나쁜 예 | 고친 예 |
|---|---|---|
| 부연 설명 | `Teams는 배포, Obsidian은 창 — 이 조합이 핵심이다` | `Teams는 배포, Obsidian은 창이다. 이 조합이 핵심이다` |
| 동격·환언 | `가장 중요한 것 — 하루 3분 — 이 안 지켜진다` | `가장 중요한 하루 3분이 안 지켜진다` |
| 결론 유도 | `점검이 사라지면 품질이 떨어진다 — 그래서 아무도 안 쓴다` | `점검이 사라지면 품질이 떨어지고, 그러면 아무도 안 쓴다` |
| 목록 뒤 요약 | `A · B · C — 셋 다 필요하다` | `A, B, C 셋 다 필요하다` |

**제목과 라벨도 바꾼다.** 1차 교정에서 결론이 이렇게 났다.

| 자리 | 처리 |
|---|---|
| 장 제목 `스케줄링 — 사람이 잊어도 돌아가게` | 콜론 → `스케줄링: 사람이 잊어도 돌아가게` |
| `<title>` 구분자 `장 제목 — 책 제목` | 중점 → `장 제목 · 책 제목` |
| 파트 라벨 `준비 — 30분 셋업` | 중점 → `준비 · 30분 셋업` |
| 표 안 짧은 라벨 `경로 B — 원격` | 중점 → `경로 B · 원격` |
| 값 없음을 뜻하는 `<td>—</td>` | `-` 또는 `해당 없음` |
| 코드 블록 안 한국어 산문 `1. 착수 — 계약 체결` | 콜론 → `1. 착수: 계약 체결` |

즉 **산문·제목·라벨·코드 안 한국어를 통틀어 `—` 는 0개**가 목표다.
`<pre>` 안이라도 한국어 문장이면 고친다. 독자가 읽는 글이기 때문이다.

## 2. 「여러분」을 쓰지 않는다

영어 `you` 의 직역이다. 한국어는 주어를 지운다.

| 나쁜 예 | 고친 예 |
|---|---|
| `여러분 회사 지식이 문서에 있다면` | `회사 지식이 문서에 있다면` |
| `여러분 볼트의 노트를 세어 보세요` | `볼트의 노트를 세어 보세요` |
| `여러분 회사 것을 직접 설계합니다` | `우리 회사 것을 직접 설계합니다` |

독자를 꼭 불러야 할 때만 「우리」를 쓴다. 이 책은 독자와 같은 편에서 쓰는 글이다.

## 3. 「~것입니다 / ~것이다」를 줄인다

명사절로 늘린 껍데기다. 대개 지우면 문장이 짧아지고 세진다.

| 나쁜 예 | 고친 예 |
|---|---|
| `이건 극단적인 예가 아닙니다` → 유지 | (이미 좋다) |
| `문제는 그게 흩어져 있다는 것입니다` | `문제는 그게 흩어져 있다는 점입니다` / `문제는 흩어져 있다는 데 있습니다` |
| `중요한 것은 지속 가능성입니다` | `중요한 건 지속 가능성입니다` (구어 축약) 또는 `지속 가능성이 중요합니다` |
| `~해야 한다는 것을 뜻합니다` | `~해야 한다는 뜻입니다` |

## 4. 「~수 있습니다」를 남용하지 않는다

영어 `can` 의 기계적 대응이다. 한국어는 그냥 서술한다.

| 나쁜 예 | 고친 예 |
|---|---|
| `AI가 폴더를 읽고 쓸 수 있습니다` | `AI가 폴더를 직접 읽고 씁니다` |
| `이렇게 적을 수 있습니다` | `이렇게 적습니다` |
| `확인할 수 있습니다` | `확인합니다` / `보입니다` |

**진짜 「가능성」을 말할 때만 남긴다**: `꺼져 있으면 그 회차는 그냥 지나갈 수 있습니다`.

## 5. 피동을 능동으로

`~되어 있습니다`, `~로 표현됩니다`, `~가 기록됩니다` 는 영어 수동태의 흔적이다.

| 나쁜 예 | 고친 예 |
|---|---|
| `권한 관리가 이미 되어 있습니다` | `권한 관리는 이미 회사 계정이 합니다` |
| `관계가 명시적으로 기록됩니다` | `관계를 노트에 직접 적습니다` |
| `노트에 적혀 있으면` | (유지 — 상태 서술은 자연스럽다) |

상태를 말하는 `적혀 있다 / 열려 있다` 는 한국어에서 자연스럽다. 굳이 안 바꾼다.
**행위자가 있는데 숨긴 문장만** 고친다.

## 6. 문두 접속사를 덜 쓴다

`그리고 / 그런데 / 그래서` 로 문단을 시작하면 영어 `And / But / So` 를 옮긴 티가 난다.
문단의 25% 이상이 접속사로 시작하면 많다.

- 앞 문장에 붙여 한 문장으로 만든다
- 접속사를 지우고 그냥 다음 문장을 쓴다 (한국어는 연결어 없이도 논리가 이어진다)
- 꼭 필요하면 문장 중간으로 옮긴다: `그래서 안 됩니다` → `그러니 안 됩니다` / `안 되는 이유가 그것입니다`

## 7. 「~적(인)」을 덜 쓴다

`구조적인 차이` → `구조가 다른 점` · `명시적으로` → `직접` · `결정론적인` → `결과가 항상 같은`
전문 용어로 굳은 것(`상대적`, `기술적`)은 남긴다.

## 8. 그 밖에 자주 걸리는 것

| 나쁜 예 | 고친 예 |
|---|---|
| `~에 대해 설명합니다` | `~을 설명합니다` |
| `~를 통해 만듭니다` | `~로 만듭니다` |
| `~의 경우에는` | `~라면` / `~일 때는` |
| `~에 있어서` | `~에서` / `~할 때` |
| `~를 진행합니다` | `~합니다` |
| `가지고 있습니다` | `있습니다` |
| `매우 중요합니다` | `중요합니다` (강조는 문장 구조로) |
| `A는 B라고 할 수 있습니다` | `A는 B입니다` |

## 8-1. 인용된 출력은 산문이 아니다

`<pre>` 안의 코드는 대상이 아니라고 이미 정했다. 그런데 **인용된 출력이 산문 안에 그대로 놓이는 경우**가 있고,
1차 배치에서 이걸 놓쳤다. gstack 입문서 ch04 에 이런 문장이 있었다.

> `"어떤 아이디어나 프로젝트에 대해 이야기하고 싶으신가요?"`

`~에 대해` 지표에 걸리지만 **AI가 화면에 실제로 출력하는 문구**다. 고치면 예시가 실물과 어긋난다.
독자는 책과 화면을 나란히 놓고 본다. 예시가 다르면 독자가 자기가 틀린 줄 안다.

**규칙: 다음은 어디에 있든 산문 규약의 대상이 아니다.**

| 대상 | 예 |
|---|---|
| AI·도구가 출력하는 문구를 인용한 것 | `"어떤 아이디어나 프로젝트에 대해…"`, `"command not found"` |
| 오류 메시지 | `net::ERR_FILE_NOT_FOUND` |
| UI 라벨을 그대로 옮긴 것 | 「이 디바이스에 항상 유지」, `Scheduled`, `New task` |
| 스킬·명령어·플래그 이름 | `/office-hours`, `--headed`, `schtasks /sc daily` |

**판별 기준: 이걸 고치면 독자가 화면에서 보는 것과 달라지나?** 그렇다면 손대지 않는다.
지표 스캔에 걸리면 예외로 보고하고 남긴다.

`<pre>` 밖에 있어 기계가 구분하지 못하므로, **검수자가 판단해서 보고해야 한다.**
자동 검사를 믿고 일괄 치환하면 이 유형이 조용히 망가진다.

## 9. 반대로 — 건드리지 말 것

이 책의 목소리는 **카페에서 동료에게 설명하는 톤**이다. 다음은 번역투가 아니라 이 책의 문체다.

- **그 책이 쓰는 종결체를 그대로 유지한다.** 「`~입니다` 로 통일」이 아니다.
  컬렉션 안에서도 책마다 다르다 — 회사용 LLM Wiki 는 `~입니다` 체,
  gstack 입문서는 `~예요 / ~어요` 반존대 설명체다. 후자를 `~입니다` 로 바꾸면 독자 대상이 달라진다.
  **다른 사람이 쓴 원고를 교정할 때는 특히 조심한다.** 어색해 보여도 번역투가 아니면 손대지 않는다
- 짧은 단문 두세 개를 연달아 던지는 리듬 (`문제는 이겁니다. 사람이 안 시킵니다.`)
- 「」 겹낫표로 용어·인용 감싸기
- 독자에게 직접 묻는 문장 (`3개월 전 결정 이유를 기억하시나요?`)
- 실패를 그대로 적는 정직함
- 기술 용어 첫 등장 시 풀어 쓰기
- 표·목록·코드 블록의 구조와 내용
- **사실·숫자·코드·프롬프트는 한 글자도 바꾸지 않는다**

## 10. 고치고 나서 확인

```
문단을 소리 내어 읽는다.
숨이 차면 문장이 길다. 리듬이 덜컹거리면 접속사가 많다.
「이 문장을 한국어로 처음 쓴 사람이 이렇게 쓸까?」를 묻는다.
```

## 11. 뜻 검수 — 문체 검수와 별도로, 반드시 한 번 더

**이 절은 실패에서 나왔다.** 1차 교정 뒤 지표(em dash 0, 태그 균형, 링크, 구조 대조)를
전부 통과했는데 독자가 이 문장에서 멈췄다.

> `"아니요... 아직 아무것도 안 물어볼 수 있어요."`

의도는 「물어볼 게 아무것도 없다」였다. 부정이 뒤집혀 뜻이 사라졌다.
**교정 전 원본에도 같은 문장이 있었다.** 에이전트가 망친 게 아니라 처음부터 잘못 쓴 문장이고,
문체 교정·구조 대조·영문 교정 세 번을 지나쳤다.

이유는 분명하다. **모든 검증이 셈이었다.** em dash 개수, 태그 짝, 링크 존재, 요소 개수 일치.
「이 문장이 뜻이 통하는가」를 세는 방법은 없다. 읽어야 한다.

### 뜻 검수에서 찾는 여섯 가지

| 유형 | 무엇 | 왜 생기나 |
|---|---|---|
| 1 | **부정·긍정 뒤집힘** — `안 ~할 수 있다`, `~않을 수 없다` | 집필 실수. 지표로는 안 잡힌다 |
| 2 | **문장 성분 누락** — 주어나 서술어가 없어 파싱이 안 됨 | em dash 삽입구를 흡수할 때 |
| 3 | **지시어 미아** — `이것`, `그게`, `마지막 줄`, `위 표`가 가리킬 대상이 없음 | 문단을 합치거나 쪼갤 때 |
| 4 | **맥락 모순** — 같은 장 앞뒤가 어긋남, 논지와 반대 | 부분만 고칠 때 |
| 5 | **개수·라벨 불일치** — 「세 가지」인데 항목이 넷 | 항목을 추가·삭제한 뒤 |
| 6 | **판본 간 뜻 차이** — 한국어판과 영어판이 다른 말을 함 | 한쪽만 고칠 때 |

### 방법

- **읽는다.** 훑지 않는다. 지표 스캔은 유형 1과 3만 부분적으로 잡는다
- **교정 전 원본과 기계 diff 를 뜬다.** 유형 2를 잡는 가장 확실한 방법이다.
  그래서 교정 전 프래그먼트를 `frag-ko-archived/` 에 보관한다
- **다른 언어판을 대조한다.** 번역자가 의도를 이해하고 조용히 바로잡았을 수 있다.
  실제로 위 깨진 문장을 영어판은 `"No... there is nothing to ask it yet."` 로 옳게 옮겼고,
  **영어판이 원문 의도를 복원하는 증거로 쓰였다**
- **뜻이 명확히 깨졌고 문맥·다른 판본·원본에서 복원 가능할 때만 고친다.** 애매하면 보고한다
- 문체가 어색한 정도는 대상이 아니다. **뜻이 안 통하는 것만** 본다

### 공정 순서 (이제 4단계다)

```
0. 원본 보관    ← 교정 전 프래그먼트를 frag-ko-archived/ 로 복사하고 봉인
1. 집필
2. 문체 교정    ← 번역투 제거. tools/check-consistency.py 로 검증
3. 뜻 검수      ← 읽어서 검증. 원본 diff + 다른 판본 대조   ★ 이 단계가 없었다
4. 구조·렌더 QA ← 태그·링크·개수·겹침. 기계로 검증
```

3번을 2번과 같은 사람(또는 같은 에이전트)이 하면 안 된다. 자기가 고친 문장은 뜻이 통해 보인다.

**0번을 빼먹으면 3번의 비용이 몇 배가 된다.** 원본이 있으면 검수자가 기계 diff 로
「교정이 뜻을 바꾸지 않았음」을 증명하고 나머지에 집중한다. 없으면 모든 문장을 처음부터 판단한다.
봉인이 필요한 이유는 따로 있다. 빌드 스크립트가 그 폴더를 읽으면 **교정 결과를 원본으로 덮어쓴다.**

### 검수자 배정은 번호가 아니라 의존 관계로 나눈다

1차에서 장 사이 자기모순 3건이 나왔는데 **전부 ch14·ch15 사이**였다.
두 장이 같은 검수자에게 배정된 덕에 발견됐다. 다른 에이전트로 갈렸으면 못 잡았다.

| 묶음 | 왜 같이 봐야 하나 |
|---|---|
| ch03 + ch09·ch10 | 온톨로지 정의와 설계 실습. 클래스·필드·어휘가 일치해야 한다 |
| ch14 + ch15 + ch16 | 자동화. 폴더 정책·필드명·프롬프트가 서로를 참조한다 |
| ch11 + ch12 + ch13 | 규칙·템플릿·스킬. CLAUDE.md 항목이 셋에 흩어진다 |
| ch17 + ch18 | 검증과 회고. 지표 정의를 공유한다 |
| ch01 + ch02 + ch19 | 문제 제기와 결론. 앞에서 약속한 것을 뒤에서 지키는지 |

한 검수자가 감당할 분량을 넘으면 쪼개되, **쪼갠 경계를 넘는 불변식은
`tools/invariants.json` 에 등록해서 기계가 대신 보게 한다.**

### 기계에 맡길 것과 사람이 읽어야 할 것

1차에서 실측한 결과다. 스크립트에 더 투자할 곳과 그러지 말아야 할 곳이 갈렸다.

| 유형 | 담당 | 근거 |
|---|---|---|
| em dash · 여러분 · 지표 예산 | 기계 | 정확히 세진다. `check-consistency.py` |
| 필드명 정본 위반 · 폴더 정책 · status 어휘 | 기계 | 불변식으로 표현 가능. 실제로 3건 잡던 것을 자동화했다 |
| hreflang 대칭 · 영어판 잔여 한글 · 경로 영문화 | 기계 | 목록 대조로 끝난다 |
| 부정 뒤집힘 | 기계 **보조** | 정확한 패턴만 잡힌다. 변형은 사람이 봐야 한다 |
| 개수 주장 vs 실제 항목 | **사람** | 정규식 정밀도 0. 실측 7건 전부 오탐(서수·기간에 걸림) |
| 지시어가 가리킬 대상 · 맥락 모순 · 판본 간 뜻 차이 | **사람** | 기계로 표현할 방법이 없다 |

**비율 예산은 분량 하한(3000자)을 걸어야 한다.** 짧은 페이지에서 1만자당 환산은 노이즈다.

### 편집 조작 금지 사항

- **인라인 태그(`<code>`, `<strong>`)가 섞인 문장에 부분 문자열 치환을 하지 않는다.**
  치환 결과가 태그 사이에 박혀 문장이 깨지고, 태그 균형 검사는 그걸 통과시킨다.
  실제로 ch15 영어판에서 발생했다. **문장 전체를 교체한다.**
- 다중 책을 훑는 도구는 보고 대상을 **저장소 상대경로**로 기록한다.
  네 책이 모두 `chapters/ch01.html` 을 가지므로 파일명으로는 집계가 조용히 틀린다.
- 불변식에는 **유효 범위**를 붙인다. 장 개수처럼 책마다 다른 값을 전역으로 두면
  다른 책에서 오탐이 쏟아진다. 실측 35건.

---

# 제2부 · 영어판 규약

영어판은 **번역이 아니라 같은 책을 영어로 쓴 것**이어야 한다.
한국어 문장을 그대로 옮기면 이번엔 반대 방향 번역투가 나온다.

## 1. 목소리

- 2인칭 `you` 를 쓴다. 한국어판이 주어를 지운 자리에 영어는 `you` 를 넣는다
- 짧은 문장. 능동태. 현재시제
- 계약서 어투 금지 (`shall`, `utilize`, `in order to`, `it should be noted that`)
- 한국어판의 따뜻한 톤을 유지한다. 미국 기술 블로그의 과장(`game-changing`, `powerful`)은 쓰지 않는다
- em dash 는 영어에서는 자연스럽다. 다만 한 단락에 두 개 이상 쓰지 않는다

## 2. 직역하면 안 되는 것

| 한국어 | 나쁜 영어 | 좋은 영어 |
|---|---|---|
| 잠깐, 정리! | `Wait, let's organize!` | `Quick recap` |
| 직접 해보기 | `Do it yourself` | `Try it` |
| 다음 장에서는... | `In the next chapter...` | `Coming up` |
| 이 장이 하는 일 | `What this chapter does` | `What this chapter is for` |
| 함정 | `Trap` | `Where this bites` / `Pitfall` |
| 쉬운 비유 | `Easy metaphor` | `A simpler way to see it` |
| 사례 · 솔빛테크 | `Case · Solbit Tech` | `Solbit Tech in practice` |
| 확인 필요 | `Need to check` | `Unverified` (필드값은 코드라 그대로 둔다) |
| 볼트 | `Vault` | `vault` (일반명사로) |

## 3. 고유명사·용어 처리

| 한국어 | 영어 |
|---|---|
| 솔빛테크 주식회사 | Solbit Tech |
| 창고봇 / 픽뷰 / 웨어트윈 | Warebot / Pickview / WareTwin |
| 대성물류 | Daesung Logistics |
| 온톨로지 / 지식 그래프 / LLM Wiki | ontology / knowledge graph / LLM Wiki |
| 제5부 자동화와 시각화 | Part 5 · Automation and Visibility |
| 부록 A 용어 사전 | Appendix A · Glossary |

## 3-1. 경로·노트 이름은 영문화한다. 필드 값은 한글로 남긴다

1차 작업에서 에이전트마다 판단이 갈렸다. 규칙을 이렇게 정한다.

**기준은 「독자가 그대로 쓸 수 있는가」다.** 영어판 독자는 자기 볼트를 영어로 만든다.
`_ops/01-온톨로지-설계서.md` 를 읽으라는 프롬프트는 그 독자의 볼트에서 작동하지 않는다.

| 대상 | 처리 | 예 |
|---|---|---|
| 폴더·파일 경로 | **영문화** | `솔빛테크 위키/` → `Solbit Tech Wiki/` · `_ops/01-온톨로지-설계서.md` → `_ops/01-ontology-design.md` · `000-홈.md` → `000-home.md` |
| 노트 이름·위키링크 | **영문화** | `[[창고봇]]` → `[[Warebot]]` · `CAP-객체인식` → `CAP-ObjectRecognition` · `PRJ-2025-스마트물류` → `PRJ-2025-SmartLogistics` |
| 필드 **이름** | 그대로 | `pdm_uses`, `confidence`, `status`, `due` |
| 필드 **값** | **한글 유지 + 첫 등장 시 괄호 설명** | `status: 운영` 유지, 산문에서 `운영 (in service)` 로 한 번 풀어준다 |
| 통제 어휘 목록 | **한글 유지** | `status ∈ {기획, 진행, 검토, 완료, 보류}` — 15장이 「진행중 / 진행 중 / 진행」이 섞이면 안 된다고 논증하는 근거라 영어로 바꾸면 논지가 사라진다 |
| 일부러 틀린 예시 | **셋이 서로 달라야 한다** | `pdm_uses` / `uses_tech` / `사용기술` → `pdm_uses` / `uses_tech` / `tech_used` |

값을 한글로 남기는 이유는 하나 더 있다. 이 책의 러닝 케이스는 **한국 회사**다.
솔빛테크의 노트에 `status: in service` 가 적혀 있으면 사례의 현실감이 사라진다.
경로는 독자가 쓰는 것이고, 값은 사례 회사가 쓰는 것이다.

## 3-2. 그 밖의 코드 처리

**필드명·코드 문법은 절대 번역하지 않는다.**
다만 프롬프트 블록 안의 **한국어 지시문은 영어로 옮긴다** (독자가 실제로 쓸 프롬프트이므로).
이때 원문 한국어 프롬프트를 주석으로 남기지 않는다. 영어판 독자는 영어 프롬프트를 쓴다.
코드 안 한국어 주석도 영어로 옮긴다.

## 4. 숫자·단위·날짜

- 날짜는 `2026-07-15` 형식 유지 (frontmatter와 일치해야 한다)
- 산문 날짜는 `July 15, 2026`
- `5~50인` → `5 to 50 people`
- `하루 3~5분` → `three to five minutes a day`
- 통화·매출 수치는 가상 수치이므로 그대로 옮긴다

## 5. SVG 다이어그램

**SVG 안의 `<text>` 도 번역한다.** 영어가 한글보다 길어지므로:
- `font-size` 를 1~1.5 줄이거나
- 문구를 짧게 줄인다 (`자동화 하나를 만들 때마다 이 순서로 묻습니다` → `Ask these three questions`)
- `text-anchor` 와 좌표는 그대로 둔다. 넘치면 문구를 더 줄인다

---

# 제3부 · 파일 규약

## 언어별 파일

같은 폴더에 `.en` 을 끼워 둔다. 경로 깊이가 같아야 공용 CSS/JS 상대경로가 그대로 먹는다.

```
building-company-llm-wiki/
├── index.html          한국어
├── index.en.html       영어
└── chapters/
    ├── ch14.html
    └── ch14.en.html
```

## 각 파일이 갖춰야 할 것

```html
<html lang="ko">                                          <!-- 영어판은 lang="en" -->
<link rel="alternate" hreflang="en" href="ch14.en.html">   <!-- 한국어판에 -->
<link rel="alternate" hreflang="ko" href="ch14.html">      <!-- 영어판에 -->
```

`book.js` 가 `link[rel=alternate][hreflang]` 를 보고 상단바에 `KO / EN` 스위처를 만든다.
**대응 파일이 없으면 스위처가 아예 안 생긴다.** 그래서 404가 나지 않는다.
